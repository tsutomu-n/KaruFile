# 図入りガイドの確認記録

2026-09-09。対象は `docs/KARUFILE_GUIDE.html` と、この変更で作成した2つのArchify図。
実行コードを変更せず、事務の仕事の例、原本維持、対象外の資料、3つの結果確認を説明する文書を作成した。

## 機械検査

- 図の型: `architecture`。各図の `validate` / `deliver`: **showcase 9/9、errors 0、warnings 0**。
- 各図の `visual-check`: **exit 0**。1440×900、1600×1000、1920×1080、2048×1320の全サイズで横・縦はみ出しなし。
- ガイド: 1440×900、1920×1080、768×1024、390×844、320×740で横はみ出しなし。本文は縦スクロールする。
- 200%拡大相当のCSS幅720でも横はみ出しなし。実機のモバイル端末検証ではない。
- 目次4項目をブラウザーでクリックし、各見出しへの移動を確認。先頭リンクも確認。
- マニュアル3リンクの相対パスと、指定されたMarkdown見出しを照合。Markdownの表示・ダウンロード方法は閲覧ソフトによる。
- HTMLを単独の一時フォルダーへコピーし、ネットワークをofflineにして開いた。4節・2図が存在し、外部HTTP(S)要求0、外部読込リソース0、スクリプト0。
- Chromeの `Page.printToPDF` でCSSのA4指定を使用。**4ページ**。PyMuPDFで全ページを125 DPIのPNGへ描画し、抽出した本文の座標が用紙内にあることを確認。
- ビルド時に元の図JSON・生成HTML・visual-checkのSHA-256を照合。正規PNG出力だけをガイドへ埋め込んだ。
- `uv run --script karufile.py --help`、文書用3スクリプトの `node --check`、`git diff --check` が成功。新規テキストも差分検査した。

最終ファイルとの対応は [guide.build.json](guide.build.json)、[guide.verify.json](guide.verify.json) と、各図のdelivery receiptを参照する。

## 目視確認

`visual_review: passed`

図の明色・暗色を1440×900と2048×1320で確認。原本／出力の対応、PDFの3分類、見出し・線・文字の欠けがない。
初期のworkflow配置では縦に長すぎたため、フォルダーと資料の対応を表すarchitecture配置に変更した。
その後の目視修正は2回（枠見出しの余白、印刷時の文字の読みやすさ）。文字サイズを下げず、箱と間隔を整理した。

ガイドのPC・狭い画面の全体画像とA4全4ページを確認した。表・図・注意事項・確認リストの欠けや不自然な途中改ページがない。
小さい画面では図も縮小されるため、同じ意味を図の代替テキストと隣接する文章でも読めるようにしている。

## 受入結果と限界

- 用途: 現場写真、会議資料、文章のスキャン、細部を保持したい図面の4例を収録。
- 原本: 入力原本を残すこと、別出力、Word・Excelと通常設定の動画はコピーしないことを明記。
- 確認: 文字・数字、色・細部、エラーの3点。比較画面とPowerShell操作を紹介し、操作の正本はマニュアルへ案内。
- オフライン: 本文と図は単独HTMLで完結。既存マニュアル自体はHTMLに内包しない。
- 印刷: ChromeのA4出力で確認。実プリンター、他OS・ブラウザー、事務員の方による読解テストは未実施。
- この文書変更に伴う、重要な未解決の表示不具合や仕様矛盾はない。実資料の圧縮Pilotは実施していない。
