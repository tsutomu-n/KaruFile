# 2026-09-13 自動スキャン・単一PDFの追補

図2と本文を現行動作へ更新。図1のフォルダー処理例はそのまま再利用した。
画像だけのスキャン（写真だけのPDFも含む）の既定300 DPIグレー圧縮、文字・OCR混在の既存動作、
原本維持、単一PDF入力を記載した。通常の文字PDFは画像化しない。

- 図2: Archify validate/deliver 9/9、errors/warnings 0、visual-checkの4画面サイズ成功。
- 1440/2048のlight/dark計4枚を目視。配置・文字・矢印に欠けや重なりなし。
- build-guide.mjs、check-guide.mjs、render-print.py成功。5画面幅、目次・リンク、単独オフライン、A4印刷を確認。
- PC/390pxのPDF節、A4全7ページの一覧と変更した3ページ目を目視。文字や図のはみ出しなし。
- 正確なHTML/PDF/図のSHAはguide.build.json、guide.verify.json、guide.a4.pages.jsonを参照。
- visual_review: passed。全文章の校正や実プリンターでの印刷検査を意味しない。

以下は過去の文書作業の履歴。過去のSHAは今回の生成物を指さない。

# 2026-09-10 文書改善と全ページ再確認

同日800px/JPEG追補: Excel既定を800px/JPEG品質72へ更新し、build/check/render-printが成功。
PC/狭幅Excel節とA4全7ページを目視確認し、文字の欠け・重なりなし。
HTML SHA `b1494aa1a3aac48d548cbcaf1349285333d74f5b422fb6a92d5edfbdfd444639`、
PDF SHA `a0b758e04f0eb61d3a0854ba976decf47984a1bdcbdd63a48e4695443444e2e8`。

マニュアルへ操作別の指定表を追加し、過去の游ゴシック実績とメイリオの実績を区別。
render-print.pyを追加し、check-guide後の印刷PNG/本文JSON/SHA/文字境界を再生成する手順を固定。
check-guide成功、render-printで7ページの文字境界合格。A4全7ページを目視し、文字切れ・重なりなし。
4種類の指定例を実際のroot CLI parserで検証。runtime変更なし、runtime suiteは再実行していない。

# 2026-09-10 置換対象と字体既定の説明統一

フォント置換は明示対象のみ、置換先メイリオは字体名省略可能、通常圧縮では置換しないと明記。
build-guide/check-guide成功、リンク・オフライン・consoleErrors=0、A4 7p。
印刷用の全7ページPNGとpages.jsonを最新PDFから再生成し、SHAと文字境界を再確認。
変更した字体節を390pxとA4第3ページで目視し文字切れ・重なりなし。全ページの目視再確認は未実施。
文書のみの変更でruntime testsは再実行していない。

# 2026-09-10 メイリオ既定化

字体節をメイリオ既定・游ゴシック明示選択へ更新。build/check成功、A4 7p。
390px字体節とA4第3ページを目視し重なり・欠落なし。証拠validation/meiryo-default-print.png。
全ページ再目視は今回未実施。

# 図入りガイドの確認記録

## 2026-09-10 Excelの追補（現行成果物）

Excelの明示選択、既定220 DPI、画像の縮小と内部データ維持、初版の配置制限、
原本保護とERRORの違い、Excelでの表示・印刷確認を第5節へ追加した。
既存のPDF・動画の説明と2図を維持し、図1は通常設定の例であることを隣接本文に明記した。

- `build-guide.mjs` / `check-guide.mjs`: 成功。HTML 796,424 bytes、SHA-256
  `50df52eebeba8e67c1bcf65bc45740a32a0555549429d05e0d2f873e76558dac`。
- 1440/1920/768/390/320pxの5画面幅と200%相当720pxで横はみ出しなし。
- 目次7項目、MANUALの6リンクの対象見出しを確認。単独offlineで7節・2図、外部HTTP(S)要求0。
- Excel節のPC・狭い画面とA4全7ページの125 DPI PNGを目視確認し、文字や図の欠けなし。
- A4の本文座標は全ページ用紙内。PDF SHA-256は
  `90714411dce19f61538140f22c458f87717929a409a2bb9dce55882025e3b462`。
- 最終の保護単位修正後は5ページ目を再目視確認し、他6ページは目視済み版とPNGバイト一致を確認。
- 文書用3スクリプトの`node --check`、root/Excel CLI help、参照先108リンク、
  Excel runtime名のignoreとxlsx fixtureの非ignore、`git diff --check`: 成功。

`visual_review: passed`。この確認は入門ガイドの表示・印刷レイアウトを対象とし、
実Excelでのブック開封・画像表示・印刷や、実資料の圧縮Pilotではない。
実プリンター・他ブラウザー・利用者による読解テストは未実施。

## 2026-09-10 動画の追補（履歴）

動画の追加設定の存在だけを示していた説明を、第4節の目的別選択へ拡張した。
圧縮／音声除去＋圧縮／任意の厳格判定、FFmpeg準備、standardとの違い、原本維持、
無音化失敗時のERRORと既存出力の残存、未対応形式、縦動画の寸法、映像内の情報は消さないこと、
共有前の映像・音声・再生ソフト確認、MANUALへの手順リンクを反映した。
既存の字体統一本文と2図は維持。図JSON/HTMLは変更していない。

### 確認結果

- `build-guide.mjs` / `check-guide.mjs`: 成功。HTML 793,998 bytes、SHA-256
  `fc4b95190bf07b3410bdf28f0e0ddcb0d3f9b9094a74f618ff54c327df132277`。
- 1440/1920/768/390/320pxの5画面幅と200%相当720pxで横はみ出しなし。
- 目次6項目、先頭リンク、MANUALの5リンクの対象見出しまで確認。
- 単独HTMLを別フォルダーに置きofflineで6節・2図を表示。外部HTTP(S)要求0、スクリプト0。
- PC・狭い画面の動画節と、A4全6ページの125 DPI PNGを目視確認。文字・図の欠けなし。
  初稿の末尾注記だけの7ページ目は、文字を縮めず印刷用余白を調整して解消した。
- PDF本文の全座標が用紙内。PDF SHAと抽出本文は`validation/guide.a4.pages.json`。
- 文書用3スクリプトの`node --check`、root CLI helpの2動画オプション、`git diff --check`: 成功。
- 一度のPNG書込みがWindowsのUNKNOWNエラーで失敗。属性と排他読書き可能性を確認後、
  build/checkを順に再実行して成功した。恒常的な原因は特定していない。

`visual_review: passed`。今回の監査範囲は動画変更に関係する現行文書。MANUAL冒頭の
未対応/不採用時の原本コピー説明にも、音声除去時の例外を反映した。
この範囲で既知の重要な説明漏れは残っていない。全機能の網羅監査ではない。
実行コードの変更、処理系pytest・compileall、実動画Pilotの再実行はしていない。
実プリンター・他ブラウザー・事務員による読解テストは未実施。
以下は当時の履歴であり、現在の6節・6ページ・5リンクの結果とは区別する。

## 2026-09-10 字体統一の追補（履歴）

前回の文書更新で、事務員向けガイドとPDFの扱いの図に字体統一の説明が漏れていたため追補した。
原稿と図JSONから再生成し、通常OFF、Windows 11の游ゴシック、日本語・英語の横書き、
画像文字には効かないこと、字形・太字・検索/コピーの変化、1冊の試し方を説明した。
結果確認には比較画面の100ページ上限と字体統一の200ページ上限を区別して記載した。
READMEから入門ガイド、ガイドからMANUALの字体統一手順への導線も追加した。

### 今回の機械検査

- 更新した図2はarchitecture、viewBox 600×320。`validate` / `deliver`はshowcase **9/9、errors 0、warnings 0**。
- 図2の`visual-check`は1440×900、1600×1000、1920×1080、2048×1320で横・縦はみ出しなし。
  図1は変更せず、9月9日の検証済みJSON・HTML・receiptをビルド時にSHA-256照合して再利用した。
- `node docs/guide/build-guide.mjs`: 成功。図1は2080×1136、図2は2400×1280の正規PNGを埋め込んだ。
  最終HTMLは790,831 bytes、SHA-256 `599c1cd96c6a4a449164ec063ce263169815db241afd5e9dccd7d58f9a87d10b`。
- `node docs/guide/check-guide.mjs`: 成功。1440、1920、768、390、320pxの5幅と、200%拡大相当の720pxで横はみ出しなし。
  目次5項目・先頭リンク・マニュアル4リンクを確認した。見出しの照合は日本語の中黒を含む句読点の除去にも対応した。
- 単独HTMLを一時フォルダーにコピーしてofflineで表示。5節・2図、外部HTTP(S)要求0、外部読込リソース0、スクリプト0、console error 0。
- Chrome `Page.printToPDF`のA4出力は**5ページ**。PyMuPDFで125 DPIへ全ページを描画し、抽出本文の座標がすべて用紙内であることを確認した。
  最終再出力の5枚のPNGも、目視確認した版とバイト単位で一致した。PDFのSHA-256と本文座標は`validation/guide.a4.pages.json`に記録。
- `uv run --script karufile.py --help`は成功し、字体統一と比較画面のオプションを確認。文書用3スクリプトの`node --check`と`git diff --check`も成功。

### 今回の目視確認と限界

`visual_review: passed`。図2は最小・最大の明暗4画像を確認し、文字切れ・重なり・矢印の混線がない。
初稿520×344は単独図の1440×900表示で縦にはみ出したため、文字サイズを下げず余白と箱幅を調整して600×320にした（修正1回）。
ガイドのPC・狭い画面の導入、図2、字体統一本文とA4全5ページを確認。本文・図の欠けや、節の途中で意図せず改ページする箇所はない。
狭い画面の縮小図は小さいため、同じ意味を隣接本文と代替テキストにも残した。実機のスマートフォン検証ではない。

現物に照らした別担当の読取レビューでも、入門説明とMANUALの間に重大な矛盾・説明漏れは見つからなかった。
MANUAL・REFERENCE・PDF_PROCESSINGに残っていた比較対象、safe制限、検証上限、採用条件、実装/検証案内の5件も同期した。
今回の変更は文書と文書用検査のみで、処理系のpytest・compileallや実資料の圧縮Pilotは再実行していない。
実プリンター、他OS・ブラウザー、事務員の方による読解テストは未実施。過去の実資料検証は別記録を参照する。

現行成果物の対応は [guide.build.json](guide.build.json)、[guide.verify.json](guide.verify.json)、
[A4描画記録](validation/guide.a4.pages.json)と図2のdelivery receiptで確認できる。
以下の初版記録の4節・4ページ・3リンクという数値は、今回の結果ではない。

## 2026-09-09 初版（履歴）

2026-09-09。対象は `docs/KARUFILE_GUIDE.html` と、この変更で作成した2つのArchify図。
実行コードを変更せず、事務の仕事の例、原本維持、対象外の資料、3つの結果確認を説明する文書を作成した。

### 機械検査

- 図の型: `architecture`。各図の `validate` / `deliver`: **showcase 9/9、errors 0、warnings 0**。
- 各図の `visual-check`: **exit 0**。1440×900、1600×1000、1920×1080、2048×1320の全サイズで横・縦はみ出しなし。
- ガイド: 1440×900、1920×1080、768×1024、390×844、320×740で横はみ出しなし。本文は縦スクロールする。
- 200%拡大相当のCSS幅720でも横はみ出しなし。実機のモバイル端末検証ではない。
- 目次4項目をブラウザーでクリックし、各見出しへの移動を確認。先頭リンクも確認。
- マニュアル3リンクの相対パスと、指定されたMarkdown見出しを照合。Markdownの表示・ダウンロード方法は閲覧ソフトによる。
- HTMLを単独の一時フォルダーへコピーし、ネットワークをofflineにして開いた。4節・2図が存在し、外部HTTP(S)要求0、外部読込リソース0、スクリプト0。
- Chromeの `Page.printToPDF` でCSSのA4指定を使用。**4ページ**。PyMuPDFで全ページを125 DPIのPNGへ描画し、抽出した本文の座標が用紙内にあることを確認。
- ビルド時に元の図JSON・生成HTML・visual-checkのSHA-256を照合。正規PNG出力だけをガイドへ埋め込んだ。
- `uv run --script karufile.py --help`、文書用3スクリプトの `node --check`、`git diff --check` が成功。新規テキストも差分検査した。

最終ファイルとの対応は [guide.build.json](guide.build.json)、[guide.verify.json](guide.verify.json) と、各図のdelivery receiptを参照する。

### 目視確認

`visual_review: passed`

図の明色・暗色を1440×900と2048×1320で確認。原本／出力の対応、PDFの3分類、見出し・線・文字の欠けがない。
初期のworkflow配置では縦に長すぎたため、フォルダーと資料の対応を表すarchitecture配置に変更した。
その後の目視修正は2回（枠見出しの余白、印刷時の文字の読みやすさ）。文字サイズを下げず、箱と間隔を整理した。

ガイドのPC・狭い画面の全体画像とA4全4ページを確認した。表・図・注意事項・確認リストの欠けや不自然な途中改ページがない。
小さい画面では図も縮小されるため、同じ意味を図の代替テキストと隣接する文章でも読めるようにしている。

### 受入結果と限界

- 用途: 現場写真、会議資料、文章のスキャン、細部を保持したい図面の4例を収録。
- 原本: 入力原本を残すこと、別出力、Word・Excelと通常設定の動画はコピーしないことを明記。
- 確認: 文字・数字、色・細部、エラーの3点。比較画面とPowerShell操作を紹介し、操作の正本はマニュアルへ案内。
- オフライン: 本文と図は単独HTMLで完結。既存マニュアル自体はHTMLに内包しない。
- 印刷: ChromeのA4出力で確認。実プリンター、他OS・ブラウザー、事務員の方による読解テストは未実施。
- この文書変更に伴う、重要な未解決の表示不具合や仕様矛盾はない。実資料の圧縮Pilotは実施していない。
# 2026-09-10 字体対応範囲の追補

タグ付きPDF・対応する透過ロゴ、欠字と注釈の制限を字体節に追記。
build-guide/check-guide成功（7節、A4 7ページ、consoleErrors=0）。
更新した390px字体節とA4第3ページを目視し、文字切れや重なりなし。
印刷証拠は `validation/font-update-print.png`。全ページの再目視は今回未実施。
# 2026-09-10 メイリオ選択の追補

字体節にメイリオ選択と「☑」対応を追記。build-guide/check-guide成功。
390px字体節とA4第3ページを目視し、文字切れ・重なりなし。A4全7ページ。
証拠は `validation/meiryo-update-print.png`。全ページ再目視は今回未実施。
